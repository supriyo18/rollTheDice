buildscript {
    ext {
        buildToolsVersion = "34.0.0"
        minSdkVersion = 23
        compileSdkVersion = 34
        targetSdkVersion = 34
        ndkVersion = "26.1.10909125"
        kotlinVersion = "1.9.22"
    }
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.0.2") // Explicit version
        classpath("com.facebook.react:react-native-gradle-plugin:0.72.3") // Replace with actual version of React Native
        classpath("org.jetbrains.kotlin:kotlin-gradle-plugin:1.9.22") // Explicit version
    }
}

apply plugin: "com.facebook.react.rootproject"
